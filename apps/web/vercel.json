{
  "version": 2,
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "cd ../.. && npx turbo run build --filter=@healthdoc/web",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}
